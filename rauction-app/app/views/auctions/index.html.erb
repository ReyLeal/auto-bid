
<div class="container-fluid" id="home-container">
  <%= render partial: "layouts/deal_auc_header" %>

<% if @auctions != nil %>
  <div class="panel-center col-md-12">
  <% @auctions.each do |f| %>
      <div class="panel col-md-6 col-md-offset-3 panel-warning">
        <div class="panel-heading clickable">
          <h3 class="panel-title"><%= f.title.capitalize %></h3>
            <span class="pull-right "><i class="glyphicon glyphicon-minus"></i></span>
          </div>
          <div class="panel-body">
            <div class="col-md-2 panel-info">
              <%= f.year %>
            </div>
            <div class="col-md-2 panel-info">
              <%= f.make %>
            </div>
            <div class="col-md-2 panel-info">
              <%= f.model %>
            </div>
            <div class="col-md-3 panel-info">
              Maximum Price: $<%= f.max_price %>
            </div>
            <div class="col-md-3 new-class-div">
            <%= link_to 'Show', user_auction_path(current_user, f.id), class:"new-class"  %>
            </div>
          </div>
        </div>
      <% end %>
      <% else %>
      <div class="">
        You havent posted any auctions yet!!
      </div>
      <% end %>

    </div>


      <!-- <div class="auctions-index text-center">
      <%# @auctions.each do |f| %>
      <%# link_to user_auction_path(current_user, f.id), class:"col-md-3 new-class list-group-item" do %>

      <%# end %>
      <%# end %>
      <a href="/"><button id="back" type="button">Back</button></a>
    </div> -->

  </div>

  <%= render partial: "footer" %>

</div>
